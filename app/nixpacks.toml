# Node 22 (18 removido do Nix). Forçar só Node — senão Deno em supabase/functions
# quebra o build (npm não existe). "providers" tem de vir ANTES de [variables].
providers = ["node"]

[variables]
NIXPACKS_NODE_VERSION = "22"
